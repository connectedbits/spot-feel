# frozen_string_literal: true

require "test_helper"

module SpotFeel
  describe :focus do
    # Using this since I couldn't get `focus` in Minitest in a gem to work
    it "should pass this focused test" do
    end
  end
end
